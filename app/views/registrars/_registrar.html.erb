<div class="registrar clearfix">
  <div class="pull-left col-md-4">
    <%= link_to registrar.name, url_for(:controller => :registrars, :action => :show, :id => registrar.id) %>
    (<%= registrar.url %>)
  </div>
  <div class="pull-left col-md-4">
    <%= pluralize(registrar.site_count, 'site') %>
  </div>
  <div class="pull-left col-md-4 text-right">
    T: <%= registrar.phone %>;
    E: <%= registrar.email %>
  </div>
</div>
